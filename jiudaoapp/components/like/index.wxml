<!-- pages/index/components/movie/index.wxml -->
<view class="container" bindtap="tapLike">
    <image class="img" src="{{liked ? likedImg : likeImg}}" />
    <text class="text">{{count}}</text>
</view>